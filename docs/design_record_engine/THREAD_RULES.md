# Codex Thread Rules

- One task = one thread.
- Never run two threads that edit the same files.
- Each thread must:
  1) restate goal
  2) list files to create/edit
  3) implement
  4) run tests
  5) report DoD status and any gaps

Codex threads guidance: :contentReference[oaicite:9]{index=9}