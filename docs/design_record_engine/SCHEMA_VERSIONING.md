# SCHEMA_VERSIONING

Rules:

All schemas use JSON Schema draft-2020-12.

Any structural change requires version bump.

Old artifacts remain valid against previous schema versions.

Schemas are append-only unless major version increment.